import { Layout, HistoryBackButton } from "@/components/clientComponents/index";

const Mypage = () => {
  return (
    <Layout>
      <HistoryBackButton />
    </Layout>
  );
};

export default Mypage;
